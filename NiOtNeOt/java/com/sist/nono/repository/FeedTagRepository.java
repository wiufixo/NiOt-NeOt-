package com.sist.nono.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.sist.nono.model.FeedTag;

public interface FeedTagRepository extends JpaRepository<FeedTag, Integer> {

}
